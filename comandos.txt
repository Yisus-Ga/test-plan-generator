# Activar (desde la raíz del proyecto)
.\.venv\Scripts\Activate

# Instalar dependencias
pip install -r requirements.txt

# Ejecutar la app
uvicorn app.main:app --reload

# Ejecutar tests
python -m pytest tests/ -v

# Desactivar
deactivate

# ========== GITHUB - Subir cambios ==========

# Ir a la raíz del proyecto
cd "c:\Users\jgallardo\OneDrive - ACCION POINT SA\Documentos\AP\IA\Agent\v5"

# Inicializar Git (solo la primera vez, si aún no es repositorio)
git init

# Ver estado (qué archivos cambiaron, qué falta subir)
git status

# Agregar todos los cambios al área de preparación
git add .

# Verificar que .env NO esté en la lista (debe estar en .gitignore)
git status

# Crear commit con un mensaje descriptivo
git commit -m "Descripción breve de los cambios"

# Agregar el repositorio remoto (solo la primera vez - reemplaza TU_USUARIO y NOMBRE_REPO)
git remote add origin https://github.com/TU_USUARIO/NOMBRE_REPO.git

# Renombrar rama a main (solo si Git lo pide)
git branch -M main

# Subir los commits a GitHub
git push -u origin main

# --- Comandos para actualizaciones posteriores (cuando ya subiste una vez) ---
# git add .
# git commit -m "Nuevo mensaje describiendo los cambios"
# git push
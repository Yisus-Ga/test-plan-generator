# Activar (desde la raíz del proyecto)
.\.venv\Scripts\Activate

# Instalar dependencias (primera vez)
pip install -r requirements.txt

# Ejecutar la app
uvicorn app.main:app --reload

# Ejecutar todos los tests
python -m pytest tests/ -v

# Solo tests unitarios (Semana 1)
python -m pytest tests/unit/ -v

# Solo tests de integración (Semana 2 + 3: servicios y endpoints)
python -m pytest tests/integration/ -v

# Solo tests de endpoints (BD en memoria)
python -m pytest tests/integration/test_endpoints.py -v

# Desactivar
deactivate

# ========== GITHUB - Subir cambios ==========



# --- Comandos para actualizaciones posteriores (cuando ya subiste una vez) ---
# git add .
# git commit -m "Nuevo mensaje describiendo los cambios"
# git push